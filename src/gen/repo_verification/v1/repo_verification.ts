// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: repo_verification/v1/repo_verification.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "repo_verification.v1";

export interface VerifyRepoRequest {
  /** Repository slug in "owner/name" form, e.g. "nodejs/node". */
  repo: string;
}

export interface VerifyRepoResponse {
  /** Always true on an OK response; absence is signalled via NOT_FOUND status. */
  exists: boolean;
}

function createBaseVerifyRepoRequest(): VerifyRepoRequest {
  return { repo: "" };
}

export const VerifyRepoRequest: MessageFns<VerifyRepoRequest> = {
  encode(message: VerifyRepoRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.repo !== "") {
      writer.uint32(10).string(message.repo);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VerifyRepoRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVerifyRepoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.repo = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VerifyRepoRequest {
    return { repo: isSet(object.repo) ? globalThis.String(object.repo) : "" };
  },

  toJSON(message: VerifyRepoRequest): unknown {
    const obj: any = {};
    if (message.repo !== "") {
      obj.repo = message.repo;
    }
    return obj;
  },

  create(base?: DeepPartial<VerifyRepoRequest>): VerifyRepoRequest {
    return VerifyRepoRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VerifyRepoRequest>): VerifyRepoRequest {
    const message = createBaseVerifyRepoRequest();
    message.repo = object.repo ?? "";
    return message;
  },
};

function createBaseVerifyRepoResponse(): VerifyRepoResponse {
  return { exists: false };
}

export const VerifyRepoResponse: MessageFns<VerifyRepoResponse> = {
  encode(message: VerifyRepoResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exists !== false) {
      writer.uint32(8).bool(message.exists);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VerifyRepoResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVerifyRepoResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.exists = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VerifyRepoResponse {
    return { exists: isSet(object.exists) ? globalThis.Boolean(object.exists) : false };
  },

  toJSON(message: VerifyRepoResponse): unknown {
    const obj: any = {};
    if (message.exists !== false) {
      obj.exists = message.exists;
    }
    return obj;
  },

  create(base?: DeepPartial<VerifyRepoResponse>): VerifyRepoResponse {
    return VerifyRepoResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VerifyRepoResponse>): VerifyRepoResponse {
    const message = createBaseVerifyRepoResponse();
    message.exists = object.exists ?? false;
    return message;
  },
};

/**
 * Internal verification service: confirms a GitHub repository exists.
 * gRPC replacement for the synchronous HTTP REST call SubscriptionService used
 * to make against the GitHub API (see src/modules/github/github.service.ts).
 */
export type RepoVerificationServiceService = typeof RepoVerificationServiceService;
export const RepoVerificationServiceService = {
  /**
   * Verifies that the given "owner/name" repository exists.
   *
   * Status codes:
   *   OK                  repo exists (exists = true)
   *   NOT_FOUND           repo does not exist
   *   INVALID_ARGUMENT    repo is empty or malformed
   *   RESOURCE_EXHAUSTED  upstream GitHub rate limit hit
   *   UNAVAILABLE         upstream GitHub unreachable
   */
  verifyRepo: {
    path: "/repo_verification.v1.RepoVerificationService/VerifyRepo" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: VerifyRepoRequest): Buffer => Buffer.from(VerifyRepoRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): VerifyRepoRequest => VerifyRepoRequest.decode(value),
    responseSerialize: (value: VerifyRepoResponse): Buffer => Buffer.from(VerifyRepoResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): VerifyRepoResponse => VerifyRepoResponse.decode(value),
  },
} as const;

export interface RepoVerificationServiceServer extends UntypedServiceImplementation {
  /**
   * Verifies that the given "owner/name" repository exists.
   *
   * Status codes:
   *   OK                  repo exists (exists = true)
   *   NOT_FOUND           repo does not exist
   *   INVALID_ARGUMENT    repo is empty or malformed
   *   RESOURCE_EXHAUSTED  upstream GitHub rate limit hit
   *   UNAVAILABLE         upstream GitHub unreachable
   */
  verifyRepo: handleUnaryCall<VerifyRepoRequest, VerifyRepoResponse>;
}

export interface RepoVerificationServiceClient extends Client {
  /**
   * Verifies that the given "owner/name" repository exists.
   *
   * Status codes:
   *   OK                  repo exists (exists = true)
   *   NOT_FOUND           repo does not exist
   *   INVALID_ARGUMENT    repo is empty or malformed
   *   RESOURCE_EXHAUSTED  upstream GitHub rate limit hit
   *   UNAVAILABLE         upstream GitHub unreachable
   */
  verifyRepo(
    request: VerifyRepoRequest,
    callback: (error: ServiceError | null, response: VerifyRepoResponse) => void,
  ): ClientUnaryCall;
  verifyRepo(
    request: VerifyRepoRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: VerifyRepoResponse) => void,
  ): ClientUnaryCall;
  verifyRepo(
    request: VerifyRepoRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: VerifyRepoResponse) => void,
  ): ClientUnaryCall;
}

export const RepoVerificationServiceClient = makeGenericClientConstructor(
  RepoVerificationServiceService,
  "repo_verification.v1.RepoVerificationService",
) as unknown as {
  new (
    address: string,
    credentials: ChannelCredentials,
    options?: Partial<ClientOptions>,
  ): RepoVerificationServiceClient;
  service: typeof RepoVerificationServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
